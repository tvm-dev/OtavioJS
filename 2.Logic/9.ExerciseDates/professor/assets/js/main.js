const h1 = document.querySelector('.container h1')
//let language = [undefined,'English', 'pt-BR', 'Spanish']
const date = new Date()
// const options = {
//     dateStyle: 'full'
// }

//h1.innerHTML = date.toLocaleDateString('pt-BR', options)

//h1.innerHTML = date.toLocaleDateString('pt-BR', { dateStyle: 'full', timeStyle: 'short' });

//The Best Way:
// h1.innerHTML = (new Intl.DateTimeFormat('pt-BR', { dateStyle: 'full', timeStyle: 'full' }).format(date));

