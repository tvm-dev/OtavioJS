/**
 * Write one function called "isLandscape" tha receive two arguments, large and height, from one imagem(like number) and returne 'true' if the image is in landscape mode!
 */

// function getLandscape(large, height){
    //     if (large > height){
        //     return true
        //     }
        //}

   
//const getLandscape = (l, h) => l > h ? true : false

const getLandscape = (l, h) => l > h
console.log(getLandscape(1280, 720))
